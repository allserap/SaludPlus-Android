<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@drawable/bg_tabs"> <TextView
    android:id="@+id/tvSheetTitle"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Reprogramar Cita"
    android:textSize="22sp"
    android:textStyle="bold"
    android:textColor="@color/citas_primary"/>

    <TextView
        android:id="@+id/tvSheetDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="📅 Fecha seleccionada: Cargando..."
        android:layout_marginTop="8dp"
        android:textColor="@color/citas_secondary"/>

    <ProgressBar
        android:id="@+id/pbSheetLoading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="24dp"
        android:visibility="visible"/>

    <TextView
        android:id="@+id/tvSheetEmpty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Lo sentimos, no hay horas disponibles para este día."
        android:textColor="#D32F2F"
        android:gravity="center"
        android:visibility="gone"/>

    <com.google.android.material.chip.ChipGroup
        android:id="@+id/cgHorasDisponibles"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        app:singleSelection="true"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnConfirmarReprogramacion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:padding="12dp"
        android:text="Confirmar Nuevo Horario"
        android:textColor="@color/citas_white"
        app:backgroundTint="@color/citas_primary"
        app:cornerRadius="10dp"
        android:enabled="false"/>
</LinearLayout>